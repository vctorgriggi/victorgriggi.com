---
import LetterGlitch from '@/components/react/LetterGlitch';
import SkillsList from '@/components/react/SkillsList';
import SocialIcons from '@/components/ui/SocialIcons.astro';
import { SOCIAL_LINKS } from '@/data/consts';
import TechSkills from './TechSkills.astro';
---

<section id="hero" class="mt-12 w-full md:mt-0">
  <div class="mx-auto max-w-5xl space-y-8 pb-14 md:py-36">
    <div class="space-y-4 text-left">
      <p class="text-md text-(--white-icon) md:text-lg">Sou o Victor.</p>

      <div class="grid grid-cols-1 gap-4 lg:grid-cols-2 lg:items-center">
        <h1
          class="text-4xl leading-tight font-medium text-pretty md:text-5xl xl:text-6xl"
        >
          Desenvolvedor <br /> Frontend
        </h1>

        <p class="text-base text-(--white-icon) sm:text-lg md:text-xl">
          Interfaces são a forma mais direta de um produto se comunicar. Eu
          trabalho para que essa <span class="shiny-sec">comunicação</span> seja clara,
          consistente e funcional.
        </p>
      </div>

      <div class="pt-3 md:pt-6">
        <SocialIcons links={SOCIAL_LINKS} class="size-8" />
      </div>
    </div>

    <TechSkills />

    <div class="grid grid-cols-1 items-center gap-8 lg:grid-cols-2">
      <SkillsList client:load />

      <div class="flex w-full justify-center overflow-hidden">
        <LetterGlitch
          client:load
          glitchColors={['#0F7DBA', '#4CB7E5', '#0B3C5D']}
          glitchSpeed={33}
          centerVignette={false}
          outerVignette={true}
          smooth={true}
        />
      </div>
    </div>
  </div>
</section>
